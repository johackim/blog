{{!--
Re-usable card for linking to posts
--}}

<article>
    <a href="{{url}}">
        {{#if feature_image}}
            <img src="{{img_url feature_image size="m"}}" alt="{{title}}" />
        {{/if}}
    </a>
</article>
